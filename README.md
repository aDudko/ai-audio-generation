# Spring AI Audio Generation

Generate audio from text at endpoint: <code>/speech</code>  
Generate text from audio at endpoint: <code>/transcript</code>

In the demo folder you will find the postman_collection